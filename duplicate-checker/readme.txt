A very fast solution to finding duplicate files within a directory and all of its subdirectores.

Just type the file path that you want starting from the drive.

Running Visually in the cmd prompt will run very slow compared to in java. It will run extremely fast in non visual mode.